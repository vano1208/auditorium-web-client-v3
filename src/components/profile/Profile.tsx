import React from 'react';
import styles from './profile.module.css';

const Profile: React.FC = () => {
  return <div className={styles.profile}>
    <div className={styles.header}>Особистий кабінет</div>
  </div>
}

export default Profile;